export const ICON_COLOR = '#ffffff';

export const BUTTON_SIZE_CLASSES: Record<string, string> = {
  sm: 'w-4 h-4 rounded-full bg-secondary items-center justify-center',
  md: 'w-5.5 h-5.5 rounded-full bg-secondary items-center justify-center',
  lg: 'w-7 h-7 rounded-full bg-secondary items-center justify-center',
};

export const TEXT_SIZE_CLASSES: Record<string, string> = {
  sm: 'text-lg font-semibold text-primary min-w-5 text-center',
  md: 'text-xl font-semibold text-primary min-w-5 text-center',
  lg: 'text-2xl font-semibold text-primary min-w-6 text-center',
};

export const ICON_SIZES: Record<string, number> = {
  sm: 12,
  md: 16,
  lg: 20,
};

export const GAP_CLASSES: Record<string, string> = {
  sm: 'gap-1',
  md: 'gap-2',
  lg: 'gap-3',
};
